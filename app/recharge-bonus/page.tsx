
import { Metadata } from 'next';
import RechargeBonusComponent from './recharge-bonus';

export const metadata: Metadata = {
  icons: null,
};

const RechargeBonusPage = () => {
  return <RechargeBonusComponent />
};

export default RechargeBonusPage;
